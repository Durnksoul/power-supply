# AGENTS.md — STM32F407 数控电源固件

本文件适用于整个 `power_supply` 仓库。使用中文沟通；工具菜单、配置项和代码标识符保留英文原名。

## 1. 项目定位与当前状态

本工程面向 STM32F407VET6 数控电源，使用 STM32CubeMX、STM32CubeF4 HAL、GNU C11、GNU Arm Embedded Toolchain、CMake 和 Ninja。

以下为 2026-09-05 的文件扫描结果。后续修改对应功能时更新，不将这些状态视为永久事实。

| 项目 | 当前证据与状态 |
| --- | --- |
| 工程与芯片 | `power_supply.ioc`：工程名 power_supply，STM32F407VET6，LQFP100 |
| 应用入口 | `Core/Src/main.c` 初始化 HAL、GPIO、SPI2 和 DisplayApp，主循环调度 LVGL |
| 时钟 | 生成代码使用 HSI，经 PLL 配置为 168 MHz；APB1 为 42 MHz，APB2 为 84 MHz。这是配置值，未经实机测量 |
| 外部晶振 | `.ioc` 为 PH0/PH1 配置 HSE 引脚，但当前生成代码的 PLL 源为 HSI；不能据引脚配置宣称正在使用 HSE |
| 业务外设 | 已配置 SPI2 显示、LCD 控制 GPIO、触摸片选与 IRQ 输入；触摸协议、电源采样、设定输出和业务串口尚未实现 |
| 软件模块 | 已接入 LVGL 9.4.0、`lcd_ili9341`、`display_app`、`ui_power` 和中文子集字体；旧 `screen_demo` 保留但不参与构建；未接入触摸、FreeRTOS、电源控制或校准 |
| 内存布局 | 链接脚本声明 Flash 512 KiB、普通 RAM 128 KiB、CCM RAM 64 KiB，不是一块连续 RAM |
| VS Code | `.vscode/settings.json` 使用 `cube-cmake`；`.settings/` 记录 STM32Cube 工具包版本 |

用户已提供 MSP2807 接线图，确认 F407 没有接入其他模块，并报告完成 J1 焊接。以本文件下方的 SPI2 接线表为准，历史 SPI1 方案作废；用户已反馈前版主界面和运行时间显示正常。其他电源项目的指标和器件方案不自动适用于本工程。

## 2. 已选方案：B，HAL 裸机 + LVGL 触控界面

用户于 2026-09-05 明确选择 B。后续围绕此方案工作，不再并行铺开简单界面版或 FreeRTOS 版。

**当前优先级：先完成屏幕控制。** 用户已反馈基础显示、英文 LVGL 主界面和右下角计时正常，并明确暂不测试触摸。当前完成中文界面、刷新率显示和相对原画面旋转 180°；电源采样和输出控制后续再做。

- 使用 HAL 和裸机主循环，以非阻塞方式调度测量处理、电源状态机、界面与必要通信。当前方案不引入 FreeRTOS。
- 显示采用 LVGL 9.4.0，输入后补。主循环通过 `DisplayApp_Process()` 调度，当前没有注册触摸输入设备。
- MCU 负责设定、测量、校准、状态机和人机交互；恒压/恒流快速环路及独立硬件保护由实际功率板承担。不擅自改成 MCU 直接驱动功率开关的数字闭环。
- 基础功能目标：电压/电流设定与读回、输出开关、故障原因与运行状态显示。温度、风扇及 CV/CC 状态按实际硬件接口接入，不虚构数据来源。
- 页面通过电源业务接口提交设定和开关请求；页面不直接改 DAC/PWM、GPIO 或保护寄存器。
- 曲线、预设、参数保存、远程控制和上位机属于待选扩展，不因可能有用就提前实现。

用户已明确授权修改当前 `.ioc`、同步生成屏幕初始化代码并实现首版驱动。CubeMX 已加载、保存并成功生成代码；Debug 编译通过。尚未授权烧录或调试器连接。不要把屏幕阶段扩展到功率级修改；后续已授权工作不重复索取授权。

## 3. 待确认硬件

以下为整个项目的硬件清单，按当前阶段确认；缺失项保持待定，不填猜测值。屏幕阶段仅需显示与输入、开发板可用引脚、供电和必要调试条件；其他引脚若已被占用需说明，功率板的详细指标与接口可以后补。

| 项目 | 需要确认 |
| --- | --- |
| 功率板 | 型号/版本、原理图或接口说明；不能仅凭项目名称认定是某款 LM5176 板 |
| 输入与输出 | 输入电源、连接器和线束；允许输出电压、电流、功率及散热条件 |
| 设定接口 | 电压/电流设定采用何种接口、有效范围与对应关系 |
| 测量接口 | 电压/电流/温度的来源、采样比例、量程、参考电压与引脚 |
| 输出与故障 | 使能有效电平、默认上下拉、故障信号及独立保护行为 |
| 显示与输入 | MSP2807，ILI9341 + XPT2046，3.3 V 供电，横屏 320×240；接线见下表，触摸与显示效果待实机验证 |
| 调试接口 | 保留的 SWD 引脚、可用串口、实际晶振及开发板资源占用 |

先形成接口与外设资源表，再核对 `.ioc`、生成代码和实际硬件。选择 B 不自动确定任何引脚或输出指标。

### 已确认的屏幕接线与首版配置

| 模块脚号 | 模块信号 | STM32 / 电源 |
| --- | --- | --- |
| 1 | VCC | 3.3 V；用户报告 J1 已焊接短接 |
| 2 | GND | GND |
| 3 | CS | PB12，LCD_CS，推挽输出，初始高 |
| 4 | RESET | PC1，LCD_RESET，推挽输出，初始高 |
| 5 | DC/RS | PC0，LCD_DC，推挽输出，初始低 |
| 6、12 | SDI(MOSI)、T_DIN | PB15，SPI2_MOSI，AF5 |
| 7、10 | SCK、T_CLK | PB13，SPI2_SCK，AF5 |
| 8 | LED | 3.3 V，背光常亮，无软件背光 GPIO |
| 9、13 | SDO(MISO)、T_DO | PB14，SPI2_MISO，AF5 |
| 11 | T_CS | PC3，TP_CS，推挽输出，初始高 |
| 14 | T_IRQ | PD2，TP_IRQ，上拉输入，当前不开 EXTI |

- SPI2：全双工主机、8 位、MSB First、CPOL Low、CPHA 1 Edge、软件 NSS、32 分频；当前 APB1 42 MHz，对应 SCK 1.3125 MHz。无 DMA、无 SPI 中断。
- PB12 必须保持普通 GPIO，不能改成硬件 NSS。触摸尚未采样，LCD 传输期间 TP_CS 始终为高。
- J1 短接后 VCC 使用 3.3 V，不切换为 5 V。第 8 脚只是背光控制，不分配 PWM。
- 旧 `screen_demo` 保留色条测试源码，当前不参与构建。当前 LVGL 传输错误后停止刷新，可在调试器查看 `display_app_status`。
- `HAL_OK` 仅表示 SPI 传输完成，不证明屏幕已响应。用户已反馈前版 LVGL 界面正常；中文、刷新率和旋转版本仍待实机确认。
- 验证记录：CubeMX 6.17.0 已重新加载并导出 `build/Debug/screen_pinout.csv`，上述八个 MCU 引脚一致；Debug 构建无编译警告，Flash 8992 B、普通 RAM 2968 B、CCM 0 B（后续修改应重新构建，不能沿用此数值）。
- CubeMX 导入仍提示原有 RCC 计算字段 `48MHZClocksFreq_Value=84000000`、`RTCHSEDivFreq_Value=12500000` 无效，以及文件未定义的虚拟 `VP_RIF_VS_RIF1` 引脚无法检索。工具仍返回 OK 并生成正确的 SPI2/GPIO；当前系统时钟代码未改变。不要将此次生成描述为无导入提示，也不要为消除提示擅自改时钟。

### 当前中文 LVGL 界面

- `Core/Src/ui_power.c` 显示数控电源、电压、电流、设定、限流、温度和输出关闭；右上角“演示”表示电源数值和输出状态只是示例，不是硬件测量或关断证据。
- 底部左侧显示刷新率（FPS），右侧显示运行时间。每次成功传完一次 LVGL 更新的最后一个分块才计数，按实际经过的毫秒折算，每秒更新统计值；不是 SPI 分块次数、面板扫描频率或最高性能测试。静态界面通常约 1 FPS，统计窗口和更新时刻可能造成波动。
- ILI9341 的 MADCTL 从 0x68 改为 0xA8，保持横屏 320×240，画面相对原方向旋转 180°。没有修改 `.ioc`、SPI2 速度或接线；以后接入触摸需匹配新方向。
- 使用单个 6400 B 分块缓冲、32 KiB LVGL 内存池；SPI 同步传输，无 DMA。分块缓冲不等于非阻塞；首屏传输约需一秒，后续接入电源调度前须评估阻塞时间。
- 中文字形位于 `ui_font_zh_16.c`，仅包含页面所需汉字，ASCII 回退至 Montserrat。生成方法见 `tools/README.md`，使用工程独立 Python 环境；禁止用本机 Python 3.12 + Pillow 9.5 执行原生字体渲染。
- 当前 Debug 编译通过：Flash 277428 B、普通 RAM 41376 B、CCM 0 B。实际 LVGL 中文渲染预览见 `build/ui-preview/preview.png`；预览不验证硬件方向或 SPI 时序。尚未由助手烧录此版。

## 4. 实施方案与阶段结果

以下是待实施计划，不代表已有功能。每阶段完成已授权工作后报告实际证据，不将例行步骤变成新的审批流程。

1. **确认屏幕接口与基础环境。** 确认屏幕型号、供电、开发板可用引脚、现有接线与显示方向，检查时钟和资源冲突，验证 Debug 构建入口；屏幕外设配置获准后实现相应初始化。本阶段不新增功率输出控制，已有功率相关引脚保持原状态。
2. **验证显示和触摸。** 先完成 LCD 基本填色、区域绘图与方向检查，再验证触摸坐标、滤波及校准；共享 SPI 时检查片选与总线切换。实际接线未确认前不修改引脚。
3. **移植 LVGL 并建立页面。** 固定版本，接入配置、时间基准、刷新回调和输入设备，使用分块缓冲。首版页面展示测量值、设定值、输出状态和故障；模拟数据必须明确标注，模拟路径不能驱动真实输出。
4. **接入真实测量。** 输出关闭时读取原始采样，完成单位换算、有效性检查与校准，用实际测量对照结果，再将有效数据接到页面和电源状态机。
5. **接入设定与输出。** 按真实功率板确定 DAC、外部 DAC、PWM 滤波或通信设定方式；先验证设定信号，再逐步验证输出开关、限值与故障关闭。UI 只调用已校验的业务接口。
6. **整机联调。** 在明确授权和实际条件下验证上下电、设定边界、输出切换、采样异常和故障恢复。带载能力、精度、温升和保护效果以测量为准。

建议的模块职责如下，仅在对应阶段需要时建立文件，不预建空模块：

| 模块 | 职责 |
| --- | --- |
| LCD/触摸驱动 | 实际硬件传输、绘图窗口、坐标获取和触摸校准 |
| LVGL 移植层 | 显示刷新、输入设备、时间基准和缓冲区管理 |
| 测量与校准 | 原始数据、量纲转换、校准参数、有效性及一致快照 |
| 电源控制 | 设定值校验、输出状态机、故障锁存与恢复条件 |
| UI | 显示状态、编辑设定、提交操作请求，不绕过控制模块 |
| 应用调度/诊断 | 主循环节拍、必要日志和模块协调 |

## 5. 开始工作与代码查询

- 先检查 Git 工作区，再读取与任务相关的 `.ioc`、头文件、源文件、CMake 和 VS Code 配置。保留已有改动，不回退、覆盖或顺手整理无关文件。
- 当前路径为 `C:/Users/soul/Desktop/STM32/power_supply`。若 Git 报 `dubious ownership`，仅为当前命令使用 `git -c safe.directory=C:/Users/soul/Desktop/STM32/power_supply ...`，不修改全局 Git 配置。
- 用户只要求“扫描”“检查”“审核”时保持只读；同时明确要求改文档或文件时，只修改授权对象。
- 直接在本工程完成任务，不另建替代工程、浏览器界面、额外前端或无关备份。
- 中文文档使用 UTF-8；PowerShell 读取时显式使用 `Get-Content -Encoding utf8`。

<!-- CODEGRAPH_START -->
### CodeGraph

CodeGraph 工具可用时，结构查询优先使用索引：

| 查询 | 工具 |
| --- | --- |
| 查找符号定义 | `codegraph_search` |
| 单个符号签名或正文 | `codegraph_node` |
| 调用者、被调用者、变更影响 | `codegraph_callers`、`codegraph_callees`、`codegraph_impact` |
| 从 X 到 Y 的完整流程 | 先 `codegraph_trace`，需要正文时再一次 `codegraph_explore` |
| 某领域上下文与多个相关符号 | 先 `codegraph_context`，再一次 `codegraph_explore` |
| 文件清单、索引健康 | `codegraph_files`、`codegraph_status` |

- 架构或调用流程问题直接用索引回答，不委派代理重复扫描，不用 grep 重建已有调用链。
- 信任正常返回的 AST 结构结果，不再用 grep 重复验证，不逐个循环读取大量符号。
- 字符串、注释、日志等字面量查询使用 `rg`；已定位文件可直接读取。工具不可调用时说明限制，使用必要的本地读取，不声称查询过索引。
- 写入后索引有约 500 ms 延迟，不在同一编辑操作后立即依赖索引新结果。
- 若服务返回 `not initialized`，询问用户：“此工程尚未初始化 CodeGraph，是否运行 `codegraph init -i` 建立索引？”不自行安装、初始化或改工具配置。
<!-- CODEGRAPH_END -->

## 6. CubeMX、生成文件与模块边界

- 未经用户明确允许，不修改 `power_supply.ioc`，不重新生成 CubeMX 工程。已有明确授权沿用，不重复索取同一授权。
- 修改 GPIO、ADC、DAC、PWM、UART、SPI、I2C、DMA 或中断前，核对 `.ioc`、`Core/Inc/main.h`、初始化函数、实际调用和硬件表；冲突先报告，不静默选择一边。
- CubeMX 管理的 Core 文件优先只修改成对 USER CODE BEGIN/END 区域。默认不改 `Drivers/`、CMSIS、启动文件、链接脚本或 `cmake/stm32cubemx/CMakeLists.txt`；任务确实涉及这些文件时先说明影响。
- 应用模块放在 `Core/Inc` 与 `Core/Src`；新增 `.c` 登记在根 `CMakeLists.txt` 的 `target_sources()` 用户区。
- `main.c` 保留初始化、启动和主循环调度，不堆积驱动或业务逻辑。
- 默认沿用 HAL。寄存器直写或 LL 应有明确时序/性能依据，并解释与 HAL 状态管理的关系。
- 改时钟后核算外设时钟、定时器倍频、PWM、ADC、串口和 SysTick；引入 USB 时另核对其所需时钟，不只看主频。

## 7. 电源控制与并发规则

- 上电、复位、初始化失败、采样失效、Error_Handler 和故障路径应让输出保持或进入关闭状态。当前空工程尚无输出关闭实现，不能因为规则存在就宣称已经关断硬件。
- 根据真实使能极性和上下拉设计关闭路径；必要外设与采样有效且无故障时才接受使能请求。故障恢复不自动恢复输出，除非用户已确认此行为及适用条件。
- 独立硬件过流、短路、过压与过温保护不能由 UI 或固件代替；输出限值服从实际输入、线束、连接器、功率级和散热条件。
- 不凭猜测交换采样通道或反转引脚去掩盖接线错误，先依据硬件表及必要的只读诊断查明差异。
- 使用定宽整数，明确毫伏、毫安、温度与原始 ADC 码的单位，检查范围、符号、溢出和校准有效性。阈值必须有硬件或测量依据。
- 区分采样无效、超量程、超时与真实零值，不将错误数据作为零值参与控制。
- 中断只做必要的数据搬运、标志和及时响应，避免格式化输出、长轮询、显示刷新和大块计算。主循环阻塞操作必须有上限，不妨碍状态机处理。
- 中断与主循环共享数据使用明确的原子访问、临界区或一致快照；`volatile` 不等于原子性，也不保证多字段快照一致。

## 8. LCD 与 LVGL 规则

- 实现前核实模块型号、电平、引脚、分辨率、方向与触摸接口。厂家其他封装/开发板的演示工程不能整套照搬。
- 先验证基本绘图，再接入固定版本的 LVGL；本次用户明确暂缓触摸测试，允许先完成显示。配置与 API 遵循同一版本，不混用大版本接口。
- LCD/触摸驱动、LVGL 移植层、页面和电源业务分开。共享 SPI 时明确片选、时序、速度切换和传输互斥。
- 图像、字体和缓冲区先做内存预算，优先分块刷新，不默认分配整屏双缓冲。启用 DMA 前核对内存可访问性和缓冲区生命周期，传输完成前不得复用。
- 在主循环周期性调用 LVGL 处理函数，控件操作集中于同一执行上下文；中断不操作控件。完成回调等接口按所选版本允许的上下文使用。
- LVGL 刷新节拍与测量/电源状态机分别规划，不能为了显示长时间阻塞控制处理。
- 设置值与测量值明确区分；CV/CC 与保护状态必须有可信硬件或业务依据，不能从页面或设定值推断。

## 9. 构建与验证

默认验证 Debug：

```powershell
cmake --preset Debug
cmake --build --preset Debug
```

- 先确认 `cmake`、`ninja`、`arm-none-eabi-gcc` 可执行。VS Code 使用 `cube-cmake` 不代表当前终端 PATH 已就绪；可查项目 `.settings/` 与已安装 STM32Cube bundles，使用已验证路径，不擅自改全局环境。
- 仅当缓存确实失效时运行 `cmake --fresh --preset Debug` 后再构建，不把清缓存作为每次默认操作。
- 仅用户要求发布或优化验证时执行 Release 配置与构建。
- 不通过重置 Git、删除工程或用户文件修复构建。必须清理时，先确认绝对目标严格位于本工程 `build/` 下。
- 每次固件代码或构建配置修改后至少完成一次相关构建，检查警告、链接结果和 Flash/RAM 使用量。纯文档修改核对内容、编码与变更范围即可，不必为此构建固件。
- 当前没有应用自动化测试。按实际改动添加有意义的验证，不为低影响文档修改引入测试框架。
- 烧录、连接调试器、串口操作与带载测试须有明确授权并确认目标设备；已授权的同一操作不重复询问。
- 编译通过不能证明外设、电源精度、时序和保护正确。实机功能结论应有相应读回、日志、波形或测量。

## 10. 交付与沟通

- 先报告结果，再说明修改文件、关键行为、实际验证和下一项必要条件。
- 明确区分文档已修改、代码已修改、编译通过、已烧录和实机已验证，不把它们混成一个完成阶段。
- 用户要求选项时给出 2–3 个明确选择及取舍；选定后只推进所选路线。
- 区分当前事实、历史规划、待确认硬件和实现建议。缺少影响电气行为的事实时具体提问，同时继续不依赖答案的已授权工作。

## 参考

- [OpenAI：AGENTS.md 项目指引](https://learn.chatgpt.com/docs/agent-configuration/agents-md)
- [LVGL：移植、主循环与线程约束](https://lvgl.io/docs/open/integration/overview)
